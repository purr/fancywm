<UserControl x:Class="FancyWM.Controls.TilingPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FancyWM.Controls"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:res="clr-namespace:FancyWM.Resources"
             xmlns:viewmodels="clr-namespace:FancyWM.ViewModels" xmlns:sys="clr-namespace:System;assembly=netstandard"
             xmlns:theme="clr-namespace:FancyWM.ThemeEngine.Wpf;assembly=FancyWM.ThemeEngine"
             d:DataContext="{d:DesignInstance Type=viewmodels:TilingPanelViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="20"
             d:DesignWidth="800">
    <Border MouseDown="OnPanelMouseDown"
            CornerRadius="{theme:CssResource panel-bar/border-radius}"
            Background="{theme:CssResource panel-bar/background}"
            Effect="{theme:CssResource panel-bar/filter}">
        <Border.OpacityMask>
            <VisualBrush>
                <VisualBrush.Visual>
                    <Border
                        Background="Black"
                        SnapsToDevicePixels="True"
                        CornerRadius="{theme:CssResource panel-bar/border-radius}"
                        Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}"
                        Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Border}}"
                    />
                </VisualBrush.Visual>
            </VisualBrush>
        </Border.OpacityMask>
        <Border Padding="0.5"
                CornerRadius="{theme:CssResource panel-bar/border-radius}"
                BorderThickness="{theme:CssResource panel-bar/border-width}"
                BorderBrush="{theme:CssResource panel-bar/border-color}"
                ClipToBounds="True">
            <Border.Background>
                <SolidColorBrush Color="{DynamicResource MicaPrimaryColor}" Opacity="{DynamicResource MicaOpacity}" />
            </Border.Background>
            <Border.Triggers>
                <EventTrigger RoutedEvent="Border.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                              Storyboard.TargetProperty="Opacity"
                              From="0.0" To="1.0" Duration="0:0:0.200" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>
            <DockPanel LastChildFill="True">
                <DockPanel.Resources>
                    <ui:SimpleStackPanel x:Key="Panellocal" Spacing="1" Orientation="Horizontal">
                        <ui:SimpleStackPanel.Resources>
                            <ResourceDictionary>
                                <Style x:Key="ActionButtonStyle" TargetType="Button">
                                    <Setter Property="Padding" Value="5,1" />
                                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Top" />
                                    <Setter Property="Background" Value="{theme:CssResource panel-bar-button/background, As=Brush}" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border
                                                    x:Name="Background"
                                                    Background="{TemplateBinding Background}"
                                                    CornerRadius="{theme:CssResource panel-bar-button/border-radius}"
                                                    SnapsToDevicePixels="True">
                                                    <Border
                                                        x:Name="Border"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        Padding="{TemplateBinding Padding}"
                                                        CornerRadius="{theme:CssResource panel-bar-button/border-radius}">
                                                        <ContentPresenter
                                                            x:Name="ContentPresenter"
                                                            TextElement.Foreground="{TemplateBinding Foreground}"
                                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                            Focusable="False"
                                                            RecognizesAccessKey="True"
                                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                                    </Border>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Background" Property="Background" Value="{DynamicResource AccentButtonBackgroundPointerOver}" />
                                                        <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentButtonBorderBrushPointerOver}" />
                                                        <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource AccentButtonForegroundPointerOver}" />
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="Background" Property="Background" Value="{DynamicResource AccentButtonBackgroundPressed}" />
                                                        <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentButtonBorderBrushPressed}" />
                                                        <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource AccentButtonForegroundPressed}" />
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter TargetName="Background" Property="Background" Value="{DynamicResource AccentButtonBackgroundDisabled}" />
                                                        <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentButtonBorderBrushDisabled}" />
                                                        <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource AccentButtonForegroundDisabled}" />
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Background" Property="Background" Value="{theme:CssResource panel-bar-button:hover/background, As=Brush}" />
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="Background" Property="Background" Value="{theme:CssResource panel-bar-button:active/background, As=Brush}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ResourceDictionary>
                        </ui:SimpleStackPanel.Resources>
                        <Border CornerRadius="{theme:CssResource panel-bar-handle/border-radius}"  Cursor="../Resources/hand_grab.cur" MouseEnter="OnHandleMouseEnter" MouseLeave="OnHandleMouseLeave" Padding="5,0,7,0" Background="{theme:CssResource panel-bar-handle/background}">
                            <StackPanel Orientation="Horizontal">
                                <local:TilingNodeCaptionBlock
                                        ExtraTextVisibility="Collapsed"
                                        VerticalAlignment="Center"
                                        Foreground="{theme:CssResource panel-bar-handle/color}"
                                        FontWeight="SemiBold"
                                        DataContext="{Binding}" />
                                <ui:FontIcon Margin="5,0,0,0" FontSize="10" FontFamily="Segoe MDL2 Assets" Glyph="&#xE970;" Foreground="{theme:CssResource panel-bar-handle/color}" />
                            </StackPanel>
                        </Border>
                        <Button MinWidth="{Binding ActualHeight, RelativeSource={RelativeSource Self}}" VerticalAlignment="Stretch" VerticalContentAlignment="Center" Style="{StaticResource ActionButtonStyle}" ToolTip="{x:Static res:Strings.Keybinding_CreateHorizontalPanel_Caption}" Command="{Binding HorizontalSplitCommand}">
                            <Viewbox Width="{DynamicResource OverlayFontSize}" Height="{DynamicResource OverlayFontSize}" Stretch="Uniform">
                                <local:SvgIcon Icon="hsplit" Color="{theme:CssResource panel-bar-button/color}" />
                            </Viewbox>
                        </Button>
                        <Button MinWidth="{Binding ActualHeight, RelativeSource={RelativeSource Self}}" VerticalAlignment="Stretch" VerticalContentAlignment="Center" Style="{StaticResource ActionButtonStyle}" ToolTip="{x:Static res:Strings.Keybinding_CreateVerticalPanel_Caption}" Command="{Binding VerticalSplitCommand}">
                            <Viewbox Width="{DynamicResource OverlayFontSize}" Height="{DynamicResource OverlayFontSize}" Stretch="Uniform">
                                <local:SvgIcon Icon="vsplit" Color="{theme:CssResource panel-bar-button/color}" />
                            </Viewbox>
                        </Button>
                        <Button MinWidth="{Binding ActualHeight, RelativeSource={RelativeSource Self}}" VerticalAlignment="Stretch" VerticalContentAlignment="Center" Style="{StaticResource ActionButtonStyle}" ToolTip="{x:Static res:Strings.Keybinding_PullWindowUp_Caption}" Command="{Binding PullUpCommand}">
                            <Viewbox Width="{DynamicResource OverlayFontSize}" Height="{DynamicResource OverlayFontSize}" Stretch="Uniform">
                                <local:SvgIcon Icon="pull-up" Color="{theme:CssResource panel-bar-button/color}" />
                            </Viewbox>
                        </Button>
                    </ui:SimpleStackPanel>
                    <Border x:Key="PanelCaption" CornerRadius="{theme:CssResource panel-bar-handle/border-radius}" Padding="5,0,7,0" Background="{theme:CssResource panel-bar-handle/background}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChildHasDirectFocus}" Value="True">
                                        <DataTrigger.Setters>
                                            <Setter Property="Opacity" Value="1" />
                                        </DataTrigger.Setters>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ChildHasDirectFocus}" Value="False">
                                        <DataTrigger.Setters>
                                            <Setter Property="Opacity" Value="0.75" />
                                        </DataTrigger.Setters>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <local:TilingNodeCaptionBlock
                            ExtraTextVisibility="Collapsed"
                            VerticalAlignment="Center"
                            Foreground="{theme:CssResource panel-bar-handle/color}"
                            FontWeight="SemiBold"
                            DataContext="{Binding}" />
                    </Border>
                </DockPanel.Resources>
                <Border DockPanel.Dock="Left" IsHitTestVisible="True" Background="Transparent">
                    <ContentPresenter x:Name="HeaderContent">
                        <ContentPresenter.Style>
                            <Style TargetType="ContentPresenter">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type Border}}}" Value="True">
                                        <Setter Property="Content" Value="{StaticResource Panellocal}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type Border}}}" Value="False">
                                        <Setter Property="Content" Value="{StaticResource PanelCaption}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentPresenter.Style>
                    </ContentPresenter>
                </Border>
                <local:TabBar ItemsSource="{Binding Path=ChildNodes}" TabMinWidth="75" TabMaxWidth="200" />
            </DockPanel>
        </Border>
    </Border>
</UserControl>
